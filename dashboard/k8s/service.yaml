apiVersion: v1
kind: Service
metadata:
  name: aegis-dashboard
  namespace: default
  labels:
    app: aegis-dashboard
spec:
  type: ClusterIP
  selector:
    app: aegis-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
