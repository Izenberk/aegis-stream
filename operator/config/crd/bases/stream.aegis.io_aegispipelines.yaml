---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: aegispipelines.stream.aegis.io
spec:
  group: stream.aegis.io
  names:
    kind: AegisPipeline
    listKind: AegisPipelineList
    plural: aegispipelines
    singular: aegispipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AegisPipeline is the Schema for the aegispipelines API.
          It represents a single deployment of the aegis-stream data router.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AegisPipelineSpec defines the desired state of AegisPipeline.
              Users write these fields in their YAML — the Operator reads them
              and creates a matching Deployment + Service.
            properties:
              costPerPodHour:
                default: "0.05"
                description: |-
                  costPerPodHour is the estimated cost in USD for running one pod per hour.
                  Used by the React dashboard to calculate live cost-per-second.
                type: string
              image:
                default: docker.io/library/aegis-stream:latest
                description: image is the container image to deploy.
                type: string
              metricsPort:
                default: 2112
                description: metricsPort is the HTTP port for Prometheus /metrics
                  and /healthz.
                format: int32
                type: integer
              port:
                default: 9000
                description: port is the TCP listen port for event traffic.
                format: int32
                type: integer
              queueDepth:
                default: 100000
                description: queueDepth is the buffered channel capacity per pod.
                format: int32
                minimum: 100
                type: integer
              replicas:
                default: 2
                description: replicas is the number of aegis-stream pods to run.
                format: int32
                maximum: 20
                minimum: 1
                type: integer
              workers:
                default: 50
                description: workers is the number of worker goroutines per pod.
                format: int32
                minimum: 1
                type: integer
            required:
            - costPerPodHour
            - image
            - metricsPort
            - port
            - queueDepth
            - replicas
            - workers
            type: object
          status:
            description: |-
              AegisPipelineStatus defines the observed state of AegisPipeline.
              The Operator updates these fields so users and the dashboard can
              see what's actually running.
            properties:
              conditions:
                description: conditions represent the detailed state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: 'phase is the current lifecycle state: Pending, Running,
                  or Failed.'
                type: string
              readyReplicas:
                description: readyReplicas is the number of pods that are currently
                  Running and healthy.
                format: int32
                type: integer
            required:
            - phase
            - readyReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
